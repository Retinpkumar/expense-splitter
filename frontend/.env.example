# Optional. Defaults to "/api", proxied to http://127.0.0.1:8000 by the
# Vite dev server (see vite.config.ts) to avoid needing CORS on the backend.
# Set this only to bypass the proxy and call an absolute backend URL.
# VITE_API_BASE_URL=http://127.0.0.1:8000
